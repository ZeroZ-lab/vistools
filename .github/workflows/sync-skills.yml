name: Build and Sync Binaries

on:
  push:
    branches:
      - main
    paths:
      - 'crates/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        include:
          - target: aarch64-apple-darwin
            os: macos-latest
            name: vistools-macos-arm64
          - target: x86_64-apple-darwin
            os: macos-latest
            name: vistools-macos-x64
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            name: vistools-linux-arm64
            linker: aarch64-linux-gnu-gcc
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            name: vistools-linux-x64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@1.88.0
        with:
          targets: ${{ matrix.target }}

      - name: Install cross-compilation tools (linux-arm64)
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Configure cross linker
        if: matrix.linker
        run: |
          mkdir -p .cargo
          echo "[target.${{ matrix.target }}]" >> .cargo/config.toml
          echo "linker = \"${{ matrix.linker }}\"" >> .cargo/config.toml

      - name: Build release binary
        run: cargo build --release --target ${{ matrix.target }}

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: target/${{ matrix.target }}/release/vistools

  sync:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Clone vistools-skills
        run: |
          git clone https://x-access-token:${{ secrets.SKILLS_REPO_TOKEN }}@github.com/ZeroZ-lab/vistools-skills.git vistools-skills
          cd vistools-skills
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Download all binaries
        uses: actions/download-artifact@v4
        with:
          path: binaries

      - name: Sync binaries to vistools-skills
        run: |
          cd vistools-skills

          # Only copy compiled binaries into skills/vistools/scripts/
          mkdir -p skills/vistools/scripts
          cp ../binaries/vistools-macos-arm64/vistools skills/vistools/scripts/vistools-macos-arm64
          cp ../binaries/vistools-macos-x64/vistools   skills/vistools/scripts/vistools-macos-x64
          cp ../binaries/vistools-linux-arm64/vistools skills/vistools/scripts/vistools-linux-arm64
          cp ../binaries/vistools-linux-x64/vistools   skills/vistools/scripts/vistools-linux-x64

          chmod +x skills/vistools/scripts/*

      - name: Commit and push if changed
        run: |
          cd vistools-skills
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Sync binaries from vistools@${{ github.sha }}"
            git push
          fi
